# CLAUDE.md

The guidelines for working in this repository live in [`AGENTS.md`](AGENTS.md). Read and follow them.

In short: edit Markdown in `notebooks/` only, never the generated HTML or `_site/`, and run `python scripts/build_site.py` to verify the build before committing.
