# Your First Note

A placeholder page. Replace it with a note of your own — this one only exists to show the template.

## Anatomy of a Note

Every notebook follows the same simple shape:

- The first `# Heading` becomes the page title and the sidebar label.
- The first line after it becomes the page description.
- Each `## Heading` becomes an entry in the table of contents.

## Write Whatever You Want

Plain prose, lists, tables, and code blocks all render:

```python
print("notes in, website out")
```

| Field | Notes |
|---|---|
| Source | Markdown in `notebooks/` |
| Output | HTML in `_site/` |

## Next Step

Copy this file, rename it with the next number (`04-...`), and start writing.
